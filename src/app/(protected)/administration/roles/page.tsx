import { permissionIds, roleIds } from "@/types/domain";
import { permissionsForRoles } from "@/lib/permissions/roles";
export default function RolesPage() { return <div className="space-y-5"><div><h1 className="text-3xl font-semibold">Roles & Permissions</h1><p className="text-[var(--muted)]">Centrally defined and read-only to prevent unsafe permission escalation.</p></div><div className="grid gap-4 lg:grid-cols-2">{roleIds.map((role) => <article key={role} className="rounded-xl border bg-white p-5"><h2 className="font-semibold capitalize">{role.replaceAll("_", " ")}</h2><p className="mt-1 text-xs text-[var(--muted)]">Organization role · controlled assignment</p><div className="mt-4 flex flex-wrap gap-2">{permissionIds.filter((permission) => permissionsForRoles([role]).has(permission)).map((permission) => <span key={permission} className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs text-emerald-800">{permission}</span>)}</div></article>)}</div></div>; }
