import { Building2, MapPin, Shield, Users, Warehouse } from "lucide-react";
import Link from "next/link";
import { PageHeader } from "@/components/ui/page-header";

const cards = [["users", "Users", "Provision accounts and manage assignments", Users], ["branches", "Branches", "Manage organization branches", Building2], ["warehouses", "Warehouses", "Manage storage facilities", Warehouse], ["locations", "Inventory locations", "Manage physical and virtual locations", MapPin], ["roles", "Roles & permissions", "Review centrally controlled access", Shield]] as const;
export default function AdministrationPage() { return <div className="page-stack"><PageHeader title="Administration" description="Secure organization configuration and access management."/><div className="card-grid">{cards.map(([path, title, description, Icon]) => <Link key={path} href={`/administration/${path}`} className="rounded-xl border bg-white p-5 transition-colors hover:border-emerald-300"><span className="mb-4 grid size-10 place-items-center rounded-lg bg-emerald-50"><Icon className="size-5 text-[var(--brand)]"/></span><h2 className="font-semibold">{title}</h2><p className="mt-1 text-sm leading-6 text-[var(--muted)]">{description}</p></Link>)}</div></div>; }
