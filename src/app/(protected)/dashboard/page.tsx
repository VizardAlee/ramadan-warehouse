import { AlertTriangle, ArrowRight, Boxes, ClipboardClock, Truck } from "lucide-react";
import Link from "next/link";
import { StatusBadge } from "@/components/ui/status-badge";

const cards = [{ label: "Open branch requests", value: "—", icon: ClipboardClock }, { label: "Transfers in progress", value: "—", icon: Truck }, { label: "Active products", value: "—", icon: Boxes }, { label: "Open discrepancies", value: "—", icon: AlertTriangle }];

export default function DashboardPage() { return <div className="space-y-8"><div><StatusBadge tone="success">Phase 1 foundation</StatusBadge><h1 className="mt-3 text-3xl font-semibold">Warehouse overview</h1><p className="mt-1 text-[var(--muted)]">Operational data will populate as product and transfer modules are enabled.</p></div><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(({ label, value, icon: Icon }) => <article key={label} className="rounded-xl border bg-white p-5 shadow-sm"><div className="flex items-start justify-between"><div><p className="text-sm text-[var(--muted)]">{label}</p><p className="mt-3 text-3xl font-semibold">{value}</p></div><span className="rounded-lg bg-emerald-50 p-2.5 text-[var(--brand)]"><Icon className="size-5"/></span></div></article>)}</section><section className="rounded-xl border bg-white p-6"><h2 className="text-lg font-semibold">Foundation ready</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-[var(--muted)]">Authentication, organization-scoped profiles, role permissions, base entities, audit infrastructure and emulator configuration are in place. Product inventory remains intentionally deferred to Phase 2.</p><Link href="/administration" className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-[var(--brand)]">Review administration foundation <ArrowRight className="size-4"/></Link></section></div>; }
