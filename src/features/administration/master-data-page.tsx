"use client";

import { zodResolver } from "@hookform/resolvers/zod";
import { Plus } from "lucide-react";
import { useState } from "react";
import { useForm, useWatch } from "react-hook-form";
import { z } from "zod";
import { Button } from "@/components/ui/button";
import { StatusBadge } from "@/components/ui/status-badge";
import { useAuth } from "@/features/auth/auth-context";
import { hasPermission } from "@/lib/permissions/roles";
import type { PermissionId, UserProfile } from "@/types/domain";
import { callAdministration } from "./api";
import { useOrganizationCollection } from "./use-organization-collection";

const schema = z.object({ name: z.string().min(2), code: z.string().regex(/^[A-Z0-9_-]{2,24}$/), state: z.string().optional(), address: z.string().optional(), contactEmail: z.union([z.string().email(), z.literal("")]).optional(), contactPhone: z.string().optional(), managerUserId: z.string().optional(), managerIds: z.array(z.string()), status: z.enum(["active", "inactive"]), type: z.enum(["warehouse", "branch", "goods_in_transit", "damaged", "quarantined", "returned"]).optional(), relatedId: z.string().optional(), systemManaged: z.boolean() });
type Values = z.infer<typeof schema>;
interface Row extends Values { id: string; warehouseId?: string; branchId?: string }
const defaults: Values = { name: "", code: "", state: "", address: "", contactEmail: "", contactPhone: "", managerUserId: "", managerIds: [], status: "active", systemManaged: false };
const configuration = { branches: { title: "Branches", callable: "saveBranch", permission: "branch.manage" }, warehouses: { title: "Warehouses", callable: "saveWarehouse", permission: "warehouse.manage" }, inventoryLocations: { title: "Inventory Locations", callable: "saveInventoryLocation", permission: "location.manage" } } as const satisfies Record<string, { title: string; callable: string; permission: PermissionId }>;

export function MasterDataPage({ collectionName }: { collectionName: keyof typeof configuration }) {
  const { profile } = useAuth(); const config = configuration[collectionName]; const records = useOrganizationCollection<Row>(collectionName); const branches = useOrganizationCollection<Row>("branches"); const warehouses = useOrganizationCollection<Row>("warehouses"); const users = useOrganizationCollection<UserProfile>("users"); const canManage = profile ? hasPermission(profile, config.permission) : false;
  const [open, setOpen] = useState(false); const [editing, setEditing] = useState<Row | null>(null); const [error, setError] = useState<string | null>(null);
  const { register, handleSubmit, control, reset, formState: { isSubmitting } } = useForm<Values>({ resolver: zodResolver(schema), defaultValues: defaults }); const type = useWatch({ control, name: "type" });
  function edit(row?: Row) { setEditing(row ?? null); reset(row ? { ...row, managerIds: row.managerIds ?? [], relatedId: row.branchId ?? row.warehouseId } : { ...defaults, type: collectionName === "inventoryLocations" ? "warehouse" : undefined }); setOpen(true); }
  const submit = handleSubmit(async (values) => { setError(null); try { const sanitized = Object.fromEntries(Object.entries(values).filter(([, value]) => value !== "" && value !== undefined)); const owner = values.type === "branch" ? { branchId: values.relatedId } : values.type === "warehouse" ? { warehouseId: values.relatedId } : {}; await callAdministration(config.callable, { ...sanitized, ...owner, id: editing?.id, idempotencyKey: crypto.randomUUID() }); setOpen(false); } catch { setError("The server rejected this configuration change."); } });
  function relationship(row: Row) { if (collectionName === "branches") return users.data.find((user) => user.id === row.managerUserId)?.displayName ?? "No manager"; if (collectionName === "warehouses") return row.managerIds.map((id) => users.data.find((user) => user.id === id)?.displayName ?? id).join(", ") || "No managers"; return branches.data.find((branch) => branch.id === row.branchId)?.name ?? warehouses.data.find((warehouse) => warehouse.id === row.warehouseId)?.name ?? "Organization virtual"; }
  return <div className="space-y-5"><div className="flex items-end justify-between"><div><h1 className="text-3xl font-semibold">{config.title}</h1><p className="text-[var(--muted)]">Organization-scoped administrative master data.</p></div>{canManage && <Button onClick={() => edit()}><Plus className="mr-2 size-4"/>Create</Button>}</div>{(error || records.error) && <p className="rounded-lg bg-red-50 p-3 text-sm text-red-800">{error ?? records.error}</p>}<div className="overflow-x-auto rounded-xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="px-4 py-3">Name</th><th className="px-4">Code</th><th className="px-4">Type / State</th><th className="px-4">Manager / Related location</th><th className="px-4">Status</th><th/></tr></thead><tbody>{records.loading ? <tr><td colSpan={6} className="p-8 text-center">Loading…</td></tr> : records.data.length === 0 ? <tr><td colSpan={6} className="p-8 text-center text-[var(--muted)]">No records configured.</td></tr> : records.data.map((row) => <tr key={row.id} className="border-t"><td className="px-4 py-3 font-medium">{row.name}{row.systemManaged && <span className="ml-2 text-xs text-amber-700">System managed</span>}</td><td className="px-4">{row.code}</td><td className="px-4">{row.type ?? row.state ?? "—"}</td><td className="px-4">{relationship(row)}</td><td className="px-4"><StatusBadge tone={row.status === "active" ? "success" : "warning"}>{row.status}</StatusBadge></td><td className="px-4">{canManage && <Button variant="ghost" onClick={() => edit(row)}>Edit</Button>}</td></tr>)}</tbody></table></div>
    {open && <div className="fixed inset-0 z-50 grid place-items-center bg-black/50 p-4"><form onSubmit={submit} className="w-full max-w-xl space-y-4 rounded-2xl bg-white p-6"><h2 className="text-xl font-semibold">{editing ? "Edit" : "Create"} {config.title}</h2><div className="grid gap-4 md:grid-cols-2"><label className="text-sm">Name<input {...register("name")} className="mt-1 w-full rounded-lg border p-2.5"/></label><label className="text-sm">Code<input {...register("code")} className="mt-1 w-full rounded-lg border p-2.5 uppercase"/></label>{collectionName !== "inventoryLocations" && <><label className="text-sm">State<input {...register("state")} className="mt-1 w-full rounded-lg border p-2.5"/></label><label className="text-sm">Address<input {...register("address")} className="mt-1 w-full rounded-lg border p-2.5"/></label></>}{collectionName === "branches" && <><label className="text-sm">Contact email<input {...register("contactEmail")} className="mt-1 w-full rounded-lg border p-2.5"/></label><label className="text-sm">Contact phone<input {...register("contactPhone")} className="mt-1 w-full rounded-lg border p-2.5"/></label><label className="text-sm">Manager<select {...register("managerUserId")} className="mt-1 w-full rounded-lg border p-2.5"><option value="">No manager</option>{users.data.map((user) => <option key={user.id} value={user.id}>{user.displayName}</option>)}</select></label></>}{collectionName === "warehouses" && <label className="text-sm">Managers<select multiple {...register("managerIds")} className="mt-1 h-28 w-full rounded-lg border p-2.5">{users.data.map((user) => <option key={user.id} value={user.id}>{user.displayName}</option>)}</select></label>}{collectionName === "inventoryLocations" && <><label className="text-sm">Location type<select {...register("type")} className="mt-1 w-full rounded-lg border p-2.5"><option value="warehouse">warehouse</option><option value="branch">branch</option><option value="goods_in_transit">goods in transit</option><option value="damaged">damaged</option><option value="quarantined">quarantined</option><option value="returned">returned</option></select></label>{(type === "branch" || type === "warehouse") && <label className="text-sm">Related {type}<select {...register("relatedId")} className="mt-1 w-full rounded-lg border p-2.5"><option value="">Select…</option>{(type === "branch" ? branches.data : warehouses.data).map((row) => <option key={row.id} value={row.id}>{row.name}</option>)}</select></label>}<label className="flex items-center gap-2 text-sm"><input type="checkbox" {...register("systemManaged")}/>System managed</label></>}<label className="text-sm">Status<select {...register("status")} className="mt-1 w-full rounded-lg border p-2.5"><option>active</option><option>inactive</option></select></label></div><div className="flex justify-end gap-3"><Button type="button" variant="secondary" onClick={() => setOpen(false)}>Cancel</Button><Button disabled={isSubmitting}>Save securely</Button></div></form></div>}
  </div>;
}
