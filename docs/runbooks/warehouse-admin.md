# Warehouse administrator

Create categories/products with normalized unique SKUs and the correct immutable tracking type. Post signed-off opening stock once through the trusted workflow, verify balances/ledger/serials/lots, then disable opening stock. For adjustments and counts, record evidence/reason, use maker-checker review, never edit a ledger record, and run reconciliation after posting.
