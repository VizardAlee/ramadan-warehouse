# Branch requester

Select only your assigned branch, add justified quantities/required date, save and review the draft, then submit the immutable version. Track approval, allocated transfers, dispatches, confirmed receipts, fulfilment, and discrepancies. Contact a branch manager for receipt issues; never create a duplicate request to bypass an in-progress transfer.
